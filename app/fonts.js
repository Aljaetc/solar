import { Jura, Comfortaa, Unbounded } from 'next/font/google'

export const jura = Jura({
  weight: '700',
  subsets: ['cyrillic'],
  display: 'swap',
})

export const comfortaa = Comfortaa({
  weight: '300',
  subsets: ['cyrillic'],
  display: 'swap',
})

export const comfortaa_bold = Comfortaa({
  weight: '700',
  subsets: ['cyrillic'],
  display: 'swap',
})

export const unboundedBold = Unbounded({
  weight: '700',
  subsets: ['cyrillic'],
  display: 'swap',
})

export const unboundedLight = Unbounded({
  weight: '200',
  subsets: ['cyrillic'],
  display: 'swap',
})

export const unbounded = Unbounded({
  weight: '400',
  subsets: ['cyrillic'],
  display: 'swap',
})